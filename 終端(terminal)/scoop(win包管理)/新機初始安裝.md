
# 安裝

```shell

# 安裝scoop (請在powerShell中執行)
# 官網: https://scoop.sh/
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
Invoke-RestMethod -Uri https://get.scoop.sh | Invoke-Expression

# 安裝git
scoop install git

# bucket 加入 extras來源 (許多包放在extras 要先加入才能安裝各種包)
scoop bucket add extras
scoop bucket list #查看bucket

# 安裝nvm (NodeJs版本管理)
# scoop search nvm
scoop install nvm
nvm install lts   # 安裝最新穩定版NodeJs
nvm list          # 查看已安裝版本
nvm use <version> # 使用版本
corepack enable   # 啟用pnpm

# 安裝pyenv (Python版本管理)
# scoop search pyenv
scoop install pyenv
pyenv install -l        # 查看可安裝版本
pyenv install <version> # 安裝Python
pyenv global <version>  # 使用版本(全域)

# 如果安裝失敗, 參考: https://github.com/pyenv-win/pyenv-win/issues/449#issuecomment-2143194236
# scoop uninstall pyenv
# scoop install pyenv (需系統管理員)
# pyenv update
# pyenv install <version>
```


# 其他指令

```shell
# 更新相關
scoop status # 查看可更新的Apps
scoop update # 更新全部安裝的Apps

# APP相關
scoop list         # 列出已安裝的pps
scoop prefix <app> # 查看安裝的App目錄
scoop shim <subcommand> [<shim_name>...] [options] [other_args] # 建立命令快捷(有些App會自動建立)

# 查詢有無可用的套件
scoop search <AppName>

# 切換使用的版本
scoop reset [package] [version]


# 搬移新電腦方式
# 舊電腦
scoop export > scoopfile.json
# 新電腦
iwr -useb get.scoop.sh | iex
scoop import scoopfile.json
```


# 其他App安裝
[參考1](https://www.cnblogs.com/fires/p/18727717)
[參考2](https://github.com/killsen/scoop-dev?tab=readme-ov-file)

```shell
# 命令列 App (必裝)
scoop install main/git
scoop install main/nvm         # NodeJs版本管理
scoop install main/pyenv       # Python版本管理

# 命令列 App (推薦)
scoop install main/go          # Go語言
scoop install main/gcc         # GNU編譯器套裝
scoop install main/ffmpeg      # 影音處理工具
scoop install main/imagemagick # 圖像處理工具

# GUI App (推薦)
scoop install extras/powertoys        # Windows擴充工具集 (若要綁定右鍵功能,安裝後會顯示需要的指令)
scoop install extras/everything       # Windows搜尋工具  shim:Everything (若要綁定右鍵功能,安裝後會顯示需要的指令)
scoop install extras/snipaste         # 螢幕擷圖軟體  shim:Snipaste
scoop install extras/potplayer        # 影音播放軟體
scoop install extras/discord          # 通訊軟體
scoop install extras/locale-emulator  # 模擬不同地區語言環境  shim:Locale-Emulator LEProc (安裝後執行 Locale Emulator Installer 完成安裝)
scoop install extras/qbittorrent      # BitTorrent用戶端  shim:qbittorrent
scoop install extras/cheat-engine     # 單機遊戲作弊工具
scoop install extras/filezilla        # FTP用戶端軟體  shim:filezilla
scoop install extras/photodemon       # 開源影像編輯工具  shim:PhotoDemon
scoop install extras/crystaldiskinfo  # 硬碟資訊檢測工具  shim:DiskInfo
scoop install extras/windows-terminal # 終端機 shim:wt WindowsTerminal (若要綁定右鍵功能,安裝後會顯示需要的指令)
	# 建議安裝 scoop install extras/vcredist2022

# GUI App (以下暫不建議)
scoop install extras/windhawk      # Windows自訂功能模組平台  shim:windhawk
	# 使用scoop安裝的windhawk 自動啟動會偏慢，所以目前仍建議官方安裝檔安裝
	# Windhawk 推薦模組: [Classic context menu on Windows 11] [Vertical Taskbar for Windows 11]
	# 開機啟動: 將exe複製捷徑加進啟動資料夾 & 捷徑右鍵內容>目標>最後面加上" -tray-only"
scoop install extras/vscode
scoop install extras/bandizip      # 多功能壓縮檔案管理工具(7.0以上版本有廣告)
scoop install extras/winrar        # 壓縮解壓縮軟體  shim:Rar UnRAR WinRAR (綁定右鍵從App中選項設定,僅英文版)
scoop install games/steam          # 未測試
# JDownloader2 (官網下載)
# Tiefsee (用 Microsoft Store 安裝)

# App (其他聽過但不常用的)
scoop install extras/obs-studio
scoop install extras/chatgpt
scoop install extras/putty
scoop install extras/x64dbg


```